{
  "name": "lts-website",
  "private": true,
  "description": "Little Technical Solutions LLC website + admin content module (Netlify Functions + Blobs) + Business Care Hub (src/).",
  "scripts": {
    "test": "node --test"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0",
    "@netlify/blobs": "^8.1.0",
    "otpauth": "^9.5.1"
  },
  "devDependencies": {
    "netlify-cli": "^26.2.0"
  }
}
